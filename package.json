{
  "name": "dondada-chatbot",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered lead qualification chatbot — Coreforge Engineering",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=18"
  }
}
